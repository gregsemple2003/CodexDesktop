# BUG-0001 PASS-0002 Live Proof Releases Overdue Schedules And Fails Codex Sandbox

## Summary

The first real `PASS-0002` worker-backed proof was not safely bounded to one run. Starting the control-plane released all three tracked schedule-triggered jobs at once, and each resulting real Codex execution then failed inside the Windows sandbox with `CreateProcessAsUserW failed: 1920`.

## Failure Evidence

- contained runtime evidence from `2026-04-07 10:29:57 America/Toronto` showed:
  - control-plane listener on `127.0.0.1:4318`
  - three fresh `codex` child processes launched together
  - three running Temporal schedule executions:
    - `codex-daily-agentic-swe-digest/schedule/00-2026-04-07T08:00:00Z`
    - `codex-daily-physical-agents-digest/schedule/00-2026-04-07T08:00:00Z`
    - `codex-daily-ue-determinism-digest/schedule/00-2026-04-07T08:00:00Z`
- run artifacts were written under:
  - `C:\Users\gregs\AppData\Local\CodexDashboard\orchestration-runs\codex-daily-agentic-swe-digest\codex-daily-agentic-swe-digest_schedule_00-2026-04-07T08_00_00Z\`
  - `C:\Users\gregs\AppData\Local\CodexDashboard\orchestration-runs\codex-daily-physical-agents-digest\codex-daily-physical-agents-digest_schedule_00-2026-04-07T08_00_00Z\`
  - `C:\Users\gregs\AppData\Local\CodexDashboard\orchestration-runs\codex-daily-ue-determinism-digest\codex-daily-ue-determinism-digest_schedule_00-2026-04-07T08_00_00Z\`
- the captured `stderr.txt` files include repeated:
  - `windows sandbox: runner error: CreateProcessAsUserW failed: 1920`

## Repro

1. Start the repo-local Temporal and Postgres stack.
2. Start the worker-backed control-plane against the current tracked `.codex` corpus.
3. Allow startup reconcile to create or update the tracked schedules.

## Observed

- the startup path releases overdue schedule work immediately instead of keeping the proof bounded to one intended trigger
- all three tracked digest jobs begin real Codex executions
- the resulting Codex executions fail inside the Windows sandbox before they can complete the skill workflow

## Expected

- a live `PASS-0002` proof can be bounded to one explicitly chosen run
- schedule reconcile does not silently release overdue work during proof setup
- the chosen real Codex execution completes far enough to provide honest end-to-end executor proof

## Current Hypothesis

- the schedule compile or create path currently leaves catchup behavior effectively open, so overdue daily schedules are eligible to fire as soon as the runtime comes up
- the separate executor failure is inside the ChatGPT-backed Codex CLI sandbox on this Windows host, not in the control-plane process

## Next Step

- fence the schedule startup path so a proof launch cannot release all overdue tracked schedules
- retry with a bounded single-run path
- if the bounded retry still hits `CreateProcessAsUserW failed: 1920`, carry that forward as the active PASS-0002 blocker with the new focused evidence
